```
sudo apt update
sudo apt install -y build-essential # 安装 gcc g++ libc-dev 等


sudo apt install -y gdb         # GNU调试器
sudo apt install -y valgrind    # 内存调试工具
sudo apt install -y cmake       # CMake构建系统
sudo apt install -y clang       # LLVM/Clang编译器
sudo apt install -y git         # 版本控制工具


sudo apt install -y manpages-dev    # 开发手册页
sudo apt install -y glibc-doc       # GNU C库文档
sudo apt install -y libstdc++-xx-doc  # C++标准库文档

sudo apt install -y libboost-all-dev  # Boost库
sudo apt install -y libssl-dev        # OpenSSL开发库
sudo apt install -y zlib1g-dev        # zlib压缩库
sudo apt install -y libsqlite3-dev    # SQLite数据库
sudo apt install -y libxml2-dev       # XML处理库
```
